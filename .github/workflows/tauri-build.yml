name: Build DevBoard

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: linux-x64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: macos-arm64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: windows-x64

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.artifact }})

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Linux system dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libfuse2t64

      - name: Install npm dependencies
        run: npm ci

      - name: Build with Tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
          APPIMAGE_EXTRACT_AND_RUN: 1
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          TAURI_UPDATER_PUBKEY: ${{ vars.TAURI_UPDATER_PUBKEY }}
        with:
          tauriScript: npx tauri
          args: --target ${{ matrix.target }}

      - name: Create fixed-name artifact (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          cp src-tauri/target/${{ matrix.target }}/release/bundle/appimage/DevBoard_*.AppImage DevBoard-Linux.AppImage
          cp src-tauri/target/${{ matrix.target }}/release/bundle/appimage/DevBoard_*.AppImage.sig DevBoard-Linux.AppImage.sig

      - name: Create fixed-name artifact (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          cp "src-tauri/target/${{ matrix.target }}/release/bundle/dmg/DevBoard_"*.dmg "DevBoard-macOS.dmg"
          cp "src-tauri/target/${{ matrix.target }}/release/bundle/macos/"*.app.tar.gz "DevBoard-macOS.app.tar.gz"
          cp "src-tauri/target/${{ matrix.target }}/release/bundle/macos/"*.app.tar.gz.sig "DevBoard-macOS.app.tar.gz.sig"

      - name: Create fixed-name artifact (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          cp src-tauri/target/${{ matrix.target }}/release/bundle/nsis/DevBoard_*-setup.exe DevBoard-Windows.exe
          cp src-tauri/target/${{ matrix.target }}/release/bundle/nsis/DevBoard_*-setup.exe.sig DevBoard-Windows.exe.sig

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: devboard-${{ matrix.artifact }}
          path: |
            DevBoard-macOS.dmg
            DevBoard-macOS.app.tar.gz
            DevBoard-macOS.app.tar.gz.sig
            DevBoard-Windows.exe
            DevBoard-Windows.exe.sig
            DevBoard-Linux.AppImage
            DevBoard-Linux.AppImage.sig
            src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
            src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage
            src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.sig
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app
            src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.tar.gz
            src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.sig
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.sig
            src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi
            src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.sig
          if-no-files-found: warn

  # Create a GitHub Release when a version tag is pushed
  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate latest.json
        run: node scripts/generate-updater-json.mjs "${{ github.ref_name }}" artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
